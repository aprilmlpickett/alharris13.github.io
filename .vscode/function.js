//make a function that puts a string into paragraph tags and returns the string with the tags
var text = ("I am from NC")

function greet(text){
  return "<p>" + text + "</p>";
   }
 
//answer given in class

function greeting(text) {
  return "<p>" + text + "</p>";
}

var text = "I am from NC";
document.write(greeting(text));